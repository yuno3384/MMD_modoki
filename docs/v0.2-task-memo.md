# v0.2 作業メモ

更新日: 2026-04-28

## 目的

v0.2 系では、v0.1 系のバグ修正ラインとは分けて、Babylon.js と babylon-mmd の新しい機能・改善を取り込む。

単なる依存更新ではなく、MMD_modoki 側で実際に価値が出る機能を有効化・検証することを目的にする。特に babylon-mmd の物理演算まわりを重めモデルでも扱いやすくする。

v0.1.9 は基本的に安定版向けのバグ修正として扱い、v0.2 系の依存更新や挙動変更を不用意に混ぜない。

このメモは、v0.2 で必ず全部実装する約束ではなく、忘れないための候補リストでもある。実装より調査・保留判断・知見の記録で止める項目があってよい。

## 作業ブランチ

- `work/v0.2-dependency-upgrade`

## ドキュメント分割

2026-05-17 以降は、方針メモと作業チェックリストを分けて管理する。

- 方針、背景、判断理由: このファイル
- 実作業のチェックボックス: [v0.2-task-checklist.md](./v0.2-task-checklist.md)

このファイル内に残っている古いチェックボックスは、当時のロードマップ履歴として扱う。
今後の完了 / 未完了の更新は `v0.2-task-checklist.md` を正本にする。

## 2026-05-17 再確認メモ

このメモは 2026-04-28 時点のロードマップから継ぎ足しているため、一部のチェック状態と実装状況がずれている。
2026-05-17 時点では、以下のように見直す。

### かなり進んでいるもの

- Babylon.js / babylon-mmd 更新:
  - `package.json` は Babylon.js `9.2.0`、babylon-mmd `^1.2.0`、Electron `40.4.1`。
  - 物理 runtime は Classic / Bullet SPR / Bullet MPR / Off の切り替え、fallback、backend 表示、physics performance log まで実装済み。
  - ただし package 版 MPR はまだ `MPR packaged build integration is pending` 扱いで、SPR fallback 前提のまま。メモ上の「package 版 MPR 統合済み」に見える記述は修正対象。
- 物理検証:
  - `MmdWasmInstanceTypeMPR` / `MmdWasmInstanceTypeSPR` 経路、Buffered 評価、重めモデル・複数モデル比較、再生・シーク・ギズモ後の安定性確認は一段落。
  - Buffered は導入可否を見たが、標準経路として強く推す段階ではない。
- 描画計測:
  - shadow render / scene render の内訳計測、frame performance log、影描画範囲の既定値見直し、モデル単位 shadow caster 切り替えは実装済み。
- Frame Graph post effects:
  - `src/render/frame-graph-post-effects-controller.ts` に post effects backend が実装済み。
  - ImageProcessing / SSAO2 / DoF / Bloom / LUT / color correction / Sharpen / Grain / Chromatic Aberration / Vignette / EdgeBlur / Lens Distortion / FXAA / SSR のタスク構成まで入っている。
  - ただし「標準描画パイプラインへ移行完了」ではなく、既存 `Scene.render` を維持した experimental backend として扱う。
- LUT:
  - Frame Graph backend 向けの独自 LUT task、LUT atlas texture、runtime text / texture key の受け渡し、built-in preset UI 復帰は実装済み。
  - `lut-file` / `lut-atlas-texture` / `lut-panel-state` の unit test がある。
  - 実機 UI での Classic との見た目比較、外部 LUT、PNG / WebM 出力反映は未確認として残す。
- MirroringFloor:
  - 水平床の専用反射床 PoC、UI、project save/load、PNG capture workaround は実装済み。
  - 可動式 MirroringPlane への一般化は未着手。
- Vitest:
  - 既に導入済みで、2026-05-17 に `npm.cmd run test:unit` は 10 files / 41 tests passed。
  - ただし Action 単位のテストや編集操作の undo/redo テストはまだない。

### まだ設計・未着手に近いもの

- Vite / Vitest major update:
  - 現在は Vite `^5.4.21`、Vitest `^2.1.9`。
  - `vite@6.x + vitest@4.x` の検証、さらに `vite@8.x` へ進むかの判断は未着手。
- Tailwind CSS:
  - まだ dependency 追加なし。
  - UI 全面移行ではなく、Action 化済み panel / settings / experimental panel から段階導入する方針だけ決まっている。
- Zustand:
  - まだ dependency 追加なし。
  - `zustand/vanilla` を UI / editor state の lightweight snapshot に限定する方針だけ決まっている。
- Action / Command / undo・redo:
  - `ActionDispatcher`、`CommandBuilder`、`HistoryManager` はまだ未実装。
  - keyboard / mouse / shortcut / gamepad / MIDI を見越した Action catalog はこれから作る。
  - SQLite WASM は undo/redo 中核から外し、必要になったら入力ログ・操作観測・クラッシュ復元の実験候補に回す。
- MMD 相当タイムライン拡張:
  - 照明トラック、アクセサリ / scene object track、セルフ影、重力、VMD 書き出しなどは未着手。
- 出力・回帰確認:
  - PMX / PMD / VMD / カメラ VMD / `.x` アクセサリの新規読み込み確認、WebGPU / WebGL2 fallback、PNG 連番、WebM、長尺出力は手動確認記録がまだ薄い。

### 当面の優先順

1. Vite / Vitest 更新検証を別コミットで行う。
2. Action catalog を作り、既存の keyframe / timeline / playback 操作を Action 名で棚卸しする。
3. `ActionDispatcher` を薄く入れ、button と shortcut が同じ Action 経路を通る操作を 1 つ作る。
4. Action 単位の Vitest を追加し、既存の手動確認だけの状態を少しずつ減らす。
5. 自前 `CommandBuilder` + in-memory `HistoryManager` で keyframe add / delete / 1f nudge の undo/redo PoC を作る。
6. Zustand は selection / editing mode / dirty / `canUndo` / `canRedo` / panel visibility に限定して導入可否を判断する。
7. Tailwind CSS は Action 化済み UI または settings / experimental panel の狭い範囲から入れる。
8. SQLite WASM は v0.2.0 の中核から外し、Action / Command の粒度が固まった後の観測ログ PoC に回す。

## 2026-04-28 時点の現在地

物理演算まわりは、v0.2 の大きな検証・分割タスクとしては一段落した。

完了済み:

- babylon-mmd 1.2.0 / Babylon.js 9.2.0 前提で、Classic と WASM/MPR を切り替え可能にした
- package 版で MPR worker helper / wasm asset を統合した
- `mmd-manager.ts` から物理 runtime 制御を分割し、Classic / WASM の両方で動作確認した
- 物理 backend 表示と physics step 計測を入れた
- 中量級モデル 4 体、最重量モデル、物理 ON/OFF で比較した
- 物理よりも shadow render / scene render 側の影響が大きいケースを確認した
- frame performance log と scene render 内訳計測を feature flag 化した
- 影描画範囲の既定値を `4800` から `1000` に縮小した
- モデル単位の遮蔽影 caster ON/OFF を情報欄から切り替え可能にした

現時点の判断:

- WASM/MPR 化は安定性と切り替え可能性の面では有効だが、FPS 改善は限定的
- 通常確認・手動テスト・回帰確認は互換性優先で Classic を基準にする
- WASM/MPR は重めモデル比較や物理まわり変更時の追加確認用として残す
- 4 体程度の中量級プロジェクトでは、物理より shadow render / camera render / DoF など描画側を優先して見る
- DoF は Frame Graph 化で触る可能性が高いため、今の段階で単独最適化を深追いしない
- 最重量モデルは MMD 本体でも極端に重いケースとして、v0.2 の主要最適化対象からは外してよい

次に優先する候補:

1. Frame Graph PoC
2. DoF / Bloom / LUT / SSAO の順序整理
3. WebGPU / WebGL2 fallback の描画回帰確認
4. MMD 相当タイムライン項目の設計再開
5. 出力確認

## 基本方針

- MMD 本体機能を優先する
- 汎用 3D アプリ化や実験機能の拡張は、依存更新の安定確認より後に回す
- 大きい依存更新は 1 グループずつ進める
- 依存を上げるだけで終わらせず、取り込む機能と検証結果を明示する
- タスクは「実装するもの」だけでなく、「調査して見送るもの」も含めて管理する
- 破綻が出たら、無理に直し切るより原因範囲と再現条件をメモに残す
- v0.1 系へ戻す可能性がある修正は、v0.2 ブランチ内でも切り分けて考える

## 開発期間の想定

v0.2 は 1 か月程度かけて開発する想定にする。

短期アップデートでは避けたい重めの機能も、この期間なら検証対象に入れてよい。ただし、v0.2 の中心はあくまで MMD 編集・再生・出力体験の改善であり、汎用機能や実験機能は優先度を下げる。

## v0.2 の主テーマ

1. babylon-mmd の新機能取り込み
2. 重めモデル向けの物理演算改善
3. 物理・描画・出力の測定と比較を残す
4. Babylon.js 9 系での WebGPU / MMD 材質 / outline 回帰確認
5. Frame Graph を v0.2 標準描画パイプライン候補として検証・移行する
6. Babylon.js 9 系で動く公式機能への置き換えと古い回避実装の整理
7. MMD 相当のタイムライン項目拡張
8. Action 単位の入力管理、undo/redo、UI state 整理を v0.2.0 の基盤整理として進める
9. Zustand / Tailwind CSS を、Action 整理とテスト拡充に合わせて段階導入する
10. SQLite WASM は undo/redo の中核ではなく、必要なら入力ログ・操作観測基盤の後続 PoC として扱う
11. さらに余力があれば、MMD 編集ワークフロー側の重め機能に着手する

優先順位:

1. 物理演算まわり: MPR / Worker / Buffered / fallback / performance log
2. 高負荷 MMD モデル対応: GPU skinning / SDEF / 多ポリゴンモデル読み込みを babylon-mmd 推奨経路へ寄せる
3. 描画パイプライン整理: Frame Graph / DoF / SSAO / 接地影補助
4. MMD 相当タイムライン項目: 照明、アクセサリ、セルフ影、重力
5. 基盤整理 PoC: Action 管理、Zustand、Tailwind CSS、undo/redo 最小実装
6. 実験 PoC: SQLite WASM 観測ログ、OBJ、点群 / Gaussian Splat

## 1か月ロードマップ案

### Week 1: 依存更新と現状維持確認

- [x] Babylon.js 9.2.0 + babylon-mmd 1.2.0 へ更新
- [x] lint / unit / package / smoke の確認
- [x] 既存プロジェクト読み込みとダンスモーション確認
- [ ] PMX / VMD / カメラ VMD / `.x` アクセサリの基本確認
- [ ] WebGPU / WebGL2 の基本確認
- [ ] 重めモデル確認用のモデル・モーション条件を決める

### Week 2: 物理 Worker / 重めモデル対応

- [x] v0.2 物理演算調査メモを作る
- [x] `MmdWasmInstanceTypeMPR` の導入実験
- [x] MPR 失敗時に SPR へ fallback する経路の設計
- [x] `PhysicsRuntimeEvaluationType.Buffered` の効果確認
- [x] GPU skinning / SDEF / 高ポリゴン PMX の推奨経路確認
- [x] 物理 backend 表示を SPR / MPR / Ammo / Off で区別
- [x] 物理 performance log の最小実装
- [x] 重めモデル 1 体と複数モデルで比較

### Week 3: 描画・エフェクト・出力回帰

- [ ] outline / toon / sphere / texture / 透過の確認
- [ ] 影 / SSAO / DoF / LUT / Fog / LuminousGlow の確認
- [x] 影描画範囲の既定値見直し
- [x] モデル単位の遮蔽影 caster ON/OFF
- [ ] Frame Graph を使った描画パイプライン PoC を作る
- [ ] Frame Graph 上で DoF / Bloom / LUT / SSAO の順序を差し替えられるか確認
- [ ] Frame Graph 上で SSR を ON / OFF できるか確認
  - `FrameGraphSSRRenderingPipelineTask` を主候補にする
  - normal / depth / reflectivity texture の生成経路を確認する
  - 全材質反射ではなく、SSR 用プリセット材質だけ反射する方式を優先候補にする
- [ ] LUT の Frame Graph 化を後段タスクとして検討する
  - `FrameGraphImageProcessingTask` 経由の `ColorGradingTexture` は import RT 構成で不安定なため、v0.2 では無理に入れない
  - 公式 `FrameGraphColorCorrectionTask` は 256x16 の 2D color table 前提で、既存 `.3dl` / `.cube` LUT とは直接互換しない
  - 必要になったら `FrameGraphCustomPostProcessTask` または独自 `FrameGraphPostProcessTask` で LUT texture / intensity を明示 bind する
- [ ] Babylon 公式 SSAO2 が WebGPU で動くか再検証
- [ ] 独自 SSAO fallback を残すか削除するか判断
- [ ] PNG 連番出力の確認
- [ ] WebM 出力の確認
- [ ] WebGPU capture 周りの遅さや破綻を記録
- [ ] MirroringFloor を可動式 MirroringPlane として拡張するか検討
  - PMX ステージ材質へ直接 `MirrorTexture` を割り当てる方式は、平面判定・法線・同一材質の複数面跨りが難しいため後回し
  - まずは専用反射板を位置・回転・サイズ調整できる形に一般化する案を優先候補にする
  - UI は床 / 壁X / 壁Z などのプリセットから始め、任意回転や複数枚対応は後続候補にする
- [ ] MMD 相当タイムライン項目の不足分を整理
- [ ] 照明トラックの最小仕様を決める
- [ ] アクセサリ / scene object track の最小仕様を決める
- [ ] SQLite-backed undo/redo PoC の対象操作を決める
- [ ] タイムラインのキー追加 / 削除 / 1f移動を履歴化できるか試す

### Week 4: 整理・仕上げ

- [x] 物理 Worker を v0.2 に入れるか、Experimental として隔離するか判断
- [ ] SQLite-backed undo/redo PoC を v0.2 に含めるか、次バージョンへ送るか判断
- [ ] Babylon 公式機能で置き換え可能になった古い回避実装を整理
- [ ] v0.2 で入れる機能と見送る機能を整理
- [ ] 既知制限を docs に反映
- [ ] 手動テスト結果を記録
- [ ] v0.2 リリース候補の確認コマンドを通す

## 重め機能候補

v0.2 の開発期間中に入れる価値が高いもの:

- [x] babylon-mmd MPR / Worker 物理
- [x] Buffered physics evaluation
- [x] 物理 performance log / 計測表示
- [x] 重めモデル向けの物理 backend 切り替え UI
- [x] 物理 backend の自動 fallback
- [ ] MMD 材質 / outline / WebGPU 回帰確認
- [ ] Frame Graph 標準化 PoC
- [ ] WebM 出力の安定性確認
- [ ] 古い独自 PostFX / workaround の棚卸し
- [ ] MirroringFloor / MirroringPlane の拡張方針整理
- [ ] SSR 用材質プリセット / Frame Graph SSR の拡張方針整理
- [ ] Action catalog 作成
- [ ] ActionDispatcher 最小導入
- [ ] Action 単位の Vitest 拡充
- [ ] in-memory HistoryManager / CommandBuilder 最小 PoC
- [ ] Zustand vanilla による UI / editor state 整理 PoC
- [ ] Tailwind CSS 段階導入 PoC
- [ ] MMD 相当タイムライン項目の拡張
- [ ] 照明キーフレーム
- [ ] アクセサリ / scene object キーフレーム

余力があれば検討するもの:

- [ ] SQLite WASM による入力ログ / 操作イベント記録 PoC
- [ ] SQLite WASM による設定変更履歴 PoC
- [ ] Property / IK トラックの保存・プレビュー
- [ ] セルフ影 / 影設定のキーフレーム
- [ ] 重力キーフレーム
- [ ] VMD 書き出しの最小 PoC
- [ ] キー範囲操作の設計
- [ ] 物理 debug 表示
- [ ] settings 画面で Experimental 機能を隔離
- [ ] OBJ 読み込みの再調査 / 最小 PoC
- [ ] 点群 / Gaussian Splat 系モデル読み込みの調査 / 最小 PoC

今回の v0.2 では優先しないもの:

- [ ] GLB / STL など汎用 3D フォーマットの本格対応
- [ ] OpenPBR / PBR カスタムシェーダー規格対応
- [ ] MIDI / Gamepad 入力拡張
- [ ] 大規模 UI 再設計

## v0.2 で取り込みたい機能

### babylon-mmd

- [x] Babylon.js 9.2.0 対応版への更新
- [x] `MmdWasmInstanceTypeMPR` を使った Worker 対応物理の導入検討
- [x] `PhysicsRuntimeEvaluationType.Buffered` の導入検討
- [x] 重めモデル / 複数モデルでの物理負荷比較
- [x] Worker 物理が使えない環境での SPR fallback
- [x] 物理 backend 表示を `Bullet SPR` / `Bullet MPR` / `Ammo` / `Off` のように区別できるか検討
- [x] 物理 debug / performance log の追加検討

判断:

- Classic / WASM は runtime 切り替え式で残す
- WASM/MPR は大幅な FPS 改善ではなく、安定性・検証余地・重めモデル対応の選択肢として扱う
- 物理 step だけでは現行の中量級 4 体ベンチの主因ではなかったため、以後は描画側の計測と Frame Graph を優先する

関連メモ:

- [v0.2-physics-investigation-note.md](./v0.2-physics-investigation-note.md)
- [physics-runtime-spec.md](./physics-runtime-spec.md)
- [physics-worker-implementation-plan.md](./physics-worker-implementation-plan.md)

### Babylon.js

- [x] Babylon.js 9.2.0 への更新
- [ ] WebGPU での MMD 材質・outline・skinning の回帰確認
- [ ] 影 / SSAO / DoF / LUT / Fog / LuminousGlow の回帰確認
- [x] shadow render / scene render 内訳の初回計測
- [x] 影描画範囲の既定値調整
- [x] モデル単位の遮蔽影 caster 切り替え
- [ ] WebGL2 fallback の回帰確認
- [ ] Babylon.js 9.2.0 以降の 9.x minor を追うか、9.2.0 に固定するか判断
- [ ] MirroringFloor を可動式 MirroringPlane へ拡張できるか検討
  - 現行は水平床専用の反射板として実装済み
  - 次段階では X/Y/Z 位置、回転、幅・奥行き、反射率、解像度を持つ scene object 的な反射プレーンを検討する
  - PMX ステージ材質へ直接反射を付ける前に、専用反射板を移動・回転できる方式で運用性を確認する
  - 関連メモ: [mirroring-floor-plan-2026-05-11.md](./mirroring-floor-plan-2026-05-11.md)
- [ ] Frame Graph SSR を実験機能として追加できるか検討
  - `FrameGraphSSRRenderingPipelineTask` を使い、Frame Graph backend 側で ON / OFF できる構成にする
  - PMX / MMD Standard material の specular をそのまま使うと意図しない反射が出るため、SSR 用プリセットシェーダー / 材質プリセットで対象を絞る
  - MirroringFloor は平面反射、SSR は画面内情報を使う材質反射として住み分ける
  - 関連メモ: [ssr-frame-graph-plan-2026-05-12.md](./ssr-frame-graph-plan-2026-05-12.md)

### Babylon.js 9 / WebGPU で再評価したい機能

Babylon.js 8 時点では WebGPU で使いにくかった機能や、MMD_modoki 側で独自実装・回避実装を持った機能を、Babylon.js 9 系で再評価する。

MMD_modoki に関係が強そうな候補:

- [ ] Clustered Lighting
  - 多数ライトの描画負荷を下げる機能
  - MMD の照明そのものは単一ライト寄りだが、ステージ / アクセサリ / 演出ライトを増やす場合に候補
- [ ] Textured Area Lights
  - 面光源に発光テクスチャを持たせる機能
  - ライブステージ、LED パネル、アクセサリ発光演出に使えるか調査
- [ ] Volumetric Lighting
  - WebGPU compute shader を活用し、WebGL2 fallback もある
  - 既存 Fog / 光芒 / LuminousGlow 系と競合または置き換え候補
- [ ] Frame Graph
  - v0.2 では標準描画パイプライン候補として扱う
  - Render pipeline を明示的に組める仕組み
  - SSAO / DoF / LUT / Bloom / 空気遠近 / WebM capture などの複雑化した後段を整理できるか調査
  - DoF を固定 RenderingPipeline 内の設定ではなく、描画タスクとして順序変更できるか確認する
  - 最初の PoC ではメイン描画全面置き換えを狙わず、既存 `Scene.render` は維持して post effect 後段だけを Frame Graph 系へ寄せる
  - 既存の描画経路は、Frame Graph PoC が MMD 材質 / outline / 透過 / WebGPU / WebGL2 fallback を満たすまで退避経路として残す
- [ ] Advanced Gaussian Splat Support
  - `.ply` / `.splat` / `.spz` / `.sog` 系
  - 点群 / Gaussian Splat 読み込み調査と接続
  - `.x` では扱いづらい現代的な背景・スキャン系素材の受け皿候補
  - MMD 本体機能ではなく scene object として隔離して試す
- [ ] Dynamic IBL Shadows
  - 環境光ベースの影
  - v0.2 では絵作り全体の IBL 表現ではなく、接地感専用機能として扱う
  - HDRI / 背景画像 / ステージ照明との連動は後回しにし、まず足元の遮蔽影だけを見る
  - RSM GI / SSGI が重い場合の代替または軽量演出候補
  - PMX モデル、`.x` アクセサリ、背景ステージで影の濃さ・接地感・ちらつきを比較する
  - MMD のセルフ影 / 地面影とは別物として扱い、置き換えではなく併用可否を見る
  - 最初の検証では HDRI を使わず、単色または固定の簡易環境で接地感だけを見る
  - 室内ステージなど、MMD の地面影だけでは足元の遮蔽感が出にくいケースを優先して確認する
  - キャラ材質への影響を抑えるため、最初は受け側 material を床・ステージに限定する
  - `shadowOpacity` は低めから始め、MMD モデルの Toon / outline / 透過へ悪影響が出ない範囲を探す
- [ ] Babylon.js 9 Outline Renderer
  - babylon-mmd 側の `MmdOutlineRenderer` と Babylon 公式 outline の役割分担を確認
  - PMX 本体は MMD outline 優先、アクセサリ / scene object は公式 outline という分け方も候補
- [ ] OpenPBR
  - PMX には直接向かないが、OBJ / GLB / scene object 材質の将来候補
  - v0.2 では原則として実装対象外にし、v0.3 以降の PBR / カスタムシェーダー対応候補として残す
  - PMX / MMD Standard material の置き換えではなく、非 MMD オブジェクト向け材質規格として扱う
- [ ] SDF Text
  - 3D ラベル、デバッグ表示、ステージ上文字表示に使えるか調査
- [ ] Inspector v2
  - 開発時の WebGPU / material / render target / post effect 調査に使えるか確認

優先して確認する順:

1. Frame Graph
2. Volumetric Lighting
3. Clustered Lighting
4. Advanced Gaussian Splat Support
5. Dynamic IBL Shadows
6. Outline Renderer

関連メモ:

- [post-effects-backlog.md](./post-effects-backlog.md)
- [frame-graph-post-effects-plan-2026-04-28.md](./frame-graph-post-effects-plan-2026-04-28.md)
- [babylon-rsm-gi-notes.md](./babylon-rsm-gi-notes.md)
- [autoluminous-glowlayer-implementation-note-2026-04-23.md](./autoluminous-glowlayer-implementation-note-2026-04-23.md)
- [generic-object-panel-design.md](./generic-object-panel-design.md)

### Frame Graph 標準化方針

v0.2 では、描画・ポストエフェクト・出力前処理の標準経路を Frame Graph ベースへ寄せられるか検証する。

ただし最初の PoC では、MMD モデル本体のメイン描画は既存経路を維持し、DoF / Bloom / LUT / SSAO / 空気遠近などの後段エフェクトだけを Frame Graph 系へ寄せる。

狙い:

- SSAO / DoF / Bloom / LUT / 空気遠近 / Fog / LuminousGlow / WebM capture の順序を明示的に管理する
- Babylon 8 時代に RenderingPipeline 内で固定されがちだった DoF などを、差し替え可能な描画タスクとして扱えるか検証する
- WebGPU と WebGL2 fallback の違いを、描画経路ごと比較できるようにする
- 既存の独自 PostFX / workaround を、公式タスクで置き換えられるものと残すものに分ける
- 中間 render target の責務を整理し、WebM 出力や高品質出力専用の経路を作りやすくする

最初の PoC:

- [ ] MMD scene color を Frame Graph 経由で最終出力する
- [ ] depth texture を使う DoF を Frame Graph 上で動かす
- [ ] DoF / Bloom / SSAO の順序を 2 パターン以上で比較する
- [ ] LUT は後段タスクとして残し、v0.2 初期 PoC では Classic backend の安定経路を優先する
- [ ] LUT を Frame Graph 化する場合は、既存 `.3dl` / `.cube` と intensity UI を活かせる独自 task 方式を検討する
- [ ] MMD outline が DoF 前後どちらに乗るべきか比較する
- [ ] PMX の透過材質、髪、スカート、アクセサリが depth / DoF で破綻しないか確認する
- [ ] WebGPU と WebGL2 fallback で同じ構成が起動できるか確認する
- [ ] WebM capture 用に、画面表示とは別の後段構成を持てるか確認する

移行条件:

- MMD Standard material / toon / sphere / texture / 透過が既存経路と大きく乖離しない
- babylon-mmd の `MmdOutlineRenderer` と競合しない
- WebGPU で起動し、WebGL2 fallback でも最低限の表示が壊れない
- 既存プロジェクト保存値の互換性を壊さない
- Frame Graph が使えない場合に、最低限の描画へ戻せる

注意:

- Frame Graph はエフェクト品質そのものを自動で上げる機能ではなく、描画タスクとリソース依存を整理する基盤として扱う
- DoF は depth texture 依存のため、透過材質や outline との順序で見た目が変わりやすい
- 空気遠近は既存 Fog とは別に、遠景を環境色へなじませる軽い color grading pass として検討する
- v0.2 では既存パイプラインの即削除ではなく、Frame Graph 経路を標準候補として作り、安定確認後に旧経路を整理する

詳細:

- [frame-graph-post-effects-plan-2026-04-28.md](./frame-graph-post-effects-plan-2026-04-28.md)

### 古い回避実装の整理

Babylon.js 8 系や当時の WebGPU 制約に合わせて無理に作った独自実装は、Babylon.js 9 系で公式経路が安定するなら置き換え候補にする。

### babylon-mmd の提供範囲メモ

2026-04-27 時点の `babylon-mmd@1.2.0` と公式ドキュメントを確認した限り、babylon-mmd が直接提供するのは主に MMD モデル / MMD アニメーション / MMD ランタイムの領域である。

babylon-mmd 側にあるもの:

- PMX / PMD / VMD / VPD / BPMX / BVMD の parser / loader
- MMD Standard material
- MMD toon shader
- sphere / toon texture など MMD 材質処理
- material morph 用 proxy
- MMD outline renderer
- SDEF injector / SDEF mesh
- WebGPU / WGSL 対応の MMD material / outline shader
- MMD camera
- model / camera animation runtime
- morph / append transform / IK
- audio sync / player control
- Bullet / Ammo / Havok 物理連携
- WASM runtime / MPR / Buffered evaluation

babylon-mmd 側の提供範囲ではなさそうなもの:

- SSAO
- Bloom
- DoF
- LUT
- Fog
- RSM GI
- AutoLuminous 相当
- MME / post effect 一式
- MMD light keyframe runtime
- self shadow keyframe runtime
- ground shadow / self shadow spec support
- アクセサリ / scene object timeline

したがって、v0.2 の古い回避実装整理では、まず次のように分ける。

- MMD 材質 / outline / SDEF / model runtime は babylon-mmd の更新に寄せる
- SSAO / DoF / Bloom / Fog / LUT / Glow などは Babylon.js 公式機能と MMD_modoki 独自実装の比較対象にする
- Light / self shadow / accessory / scene object timeline は MMD_modoki 側の `modoki-owned tracks` として扱う

参考:

- babylon-mmd README / implementation status
- babylon-mmd VMD/VPD behavior docs
- babylon-mmd WASM runtime docs
- babylon-mmd physics docs

最初に見る候補:

- [ ] SSAO
  - 現行は Babylon 標準 `SSAO2RenderingPipeline` を主経路にできず、独自 fullscreen / shader 経路を持っている
  - Babylon.js 9.2.0 で WebGPU + PrePass / MRT / SSAO2 が安定するか確認する
  - 公式 SSAO2 が WebGPU / WebGL2 の両方で十分動くなら、独自 `mmdSimpleSsao` / `ssao-shader.ts` / fallback PostProcess の削除を検討する
  - 公式 SSAO2 が MMD Standard の Toon 寄せ合成を代替できない場合は、独自経路を Experimental または MMD 専用モードとして残す
- [ ] DoF / LensRenderingPipeline 周辺
- [ ] Glow / LuminousGlow 周辺
- [ ] RSM GI / SSGI 周辺
  - 以前の RSM GI 試作は、material plugin の初期化順、WebGPU 拡張、見た目の変化が分かりづらい問題で安定運用まで行けなかった
  - Babylon.js 9 系で公式 GI 経路を再確認する
  - 既存 `src/render/global-illumination-controller.ts` は延命より書き直し候補として扱う
  - 書き直す場合は、最初から Experimental 扱いにして、PMX 材質 / アクセサリ / 背景との相性を個別確認する
- [ ] shadow / post effect の Babylon 9 系公式機能との差分

判断基準:

- 公式経路で WebGPU と WebGL2 の両方が起動するか
- PMX / VMD / カメラ VMD / 背景 / アクセサリと同時に使って破綻しないか
- MMD Standard の見た目に必要な独自合成を失わないか
- project 保存 / 読み込みの互換性を保てるか
- コード量と保守負担を実際に減らせるか

関連メモ:

- [ssao-current-spec.md](./ssao-current-spec.md)
- [ssao-webgpu-investigation.md](./ssao-webgpu-investigation.md)
- [post-effects-backlog.md](./post-effects-backlog.md)
- [babylon-rsm-gi-notes.md](./babylon-rsm-gi-notes.md)

### Action / Zustand / Tailwind CSS 基盤整理

v0.2.0 の長めの開発期間では、入力管理と UI state 整理を基盤タスクとして扱う。

方針:

- 入力は `physical input -> binding -> Action -> Command / Service` の形に寄せる
- undo/redo は SQLite WASM ではなく、まず in-memory HistoryManager + command diff で試す
- Zustand は UI / editor state の lightweight snapshot に限定して使う
- Tailwind CSS は Action 化済み UI または設定画面から段階導入する
- shortcut custom / gamepad / MIDI は後続だが、`source` / `device` / `scope` は Action catalog に入れておく

作業順:

- [ ] `library-adoption-investigation-v0.2-2026-05-17.md` を v0.2.0 の基盤整理メモとして参照する
- [ ] Action catalog を作る
  - [ ] `playback.*`
  - [ ] `timeline.*`
  - [ ] `keyframe.*`
  - [ ] `selection.*`
  - [ ] `viewport.*`
  - [ ] `project.*`
  - [ ] `effect.*`
- [ ] Action に `source` / `device` / `scope` の考え方を入れる
- [ ] keyboard shortcut custom / gamepad / MIDI 向けの `InputBinding` 最小案を作る
- [ ] keyframe / timeline 周辺に `ActionDispatcher` を薄く入れる
  - [ ] keyframe add
  - [ ] keyframe delete
  - [ ] selected keyframe nudge
  - [ ] interpolation preset apply / reset
- [ ] button と shortcut が同じ Action 経路を通る操作を作る
- [ ] Action 単位の Vitest を追加する
  - [ ] `Action -> canExecute`
  - [ ] `Action -> Command`
  - [ ] `Action -> diff`
  - [ ] undo / redo 用の最小差分
  - [ ] `mergeKey`
- [ ] 自前 `CommandBuilder` の最小 PoC を作る
- [ ] 自前 in-memory `HistoryManager` の最小 PoC を作る
- [ ] `Ctrl+Z` / `Ctrl+Y` または仮 API で undo / redo を確認する
- [ ] undo / redo 後に source animation / timeline 表示 / Babylon runtime 状態が揃うか確認する
- [ ] Zustand vanilla を必要な範囲だけ導入する
  - [ ] selection snapshot
  - [ ] editing mode
  - [ ] dirty flag
  - [ ] `canUndo` / `canRedo`
  - [ ] panel visibility
- [ ] Zustand に入れない state を明文化する
  - [ ] Babylon object
  - [ ] MMD model 実体
  - [ ] 大きな animation buffer
  - [ ] project save format
  - [ ] physics runtime state
  - [ ] 毎フレーム更新値
- [ ] Tailwind CSS を段階導入する対象 UI を決める
  - [ ] Action 化済み panel
  - [ ] 設定画面
  - [ ] experimental panel
- [ ] Tailwind 導入後に既存 UI の見た目と panel layout を確認する
- [ ] Tailwind 導入後に `smoke:launch` / `package` を確認する

関連メモ:

- [library-adoption-investigation-v0.2-2026-05-17.md](./library-adoption-investigation-v0.2-2026-05-17.md)
- [undo-redo-investigation.md](./undo-redo-investigation.md)
- [mmd-shortcuts-research.md](./mmd-shortcuts-research.md)

### SQLite WASM / undo・入力ログ基盤

SQLite WASM は v0.2.0 では undo/redo の中核にしない。まず Action / Command / in-memory HistoryManager を優先し、SQLite は必要になったら観測ログやクラッシュ復元の後続 PoC として扱う。

- [ ] SQLite WASM を実験用の独立レイヤとして導入できるか確認
- [ ] 履歴 1 件の単位を `command_event` として記録できるか確認する
- [ ] `input_event` / `command_event` / `error_event` の最小スキーマ案を作る
- [ ] 高頻度 input logging が UI / 再生 / WebGPU 描画へ与える負荷を測る
- [ ] 既存の `electron-log` と役割分担を整理する
- [ ] 操作観測・クラッシュ復元・補助保存先としても使えるか検証する
- [ ] 実験機能として settings / feature flag で隔離できるか検討する

関連メモ:

- [sqlite-wasm-experiment-note.md](./sqlite-wasm-experiment-note.md)
- [undo-redo-investigation.md](./undo-redo-investigation.md)
- [logging-introduction-note.md](./logging-introduction-note.md)

### 汎用モデル / 点群読み込み調査

`.x` アクセサリは既存 MMD 資産との相性はよいが、今後の背景・小物・スキャン素材の取り回しには限界がある。
v0.2 では本格的な汎用 3D アプリ化ではなく、`.x` の代替導線として OBJ と点群 / Gaussian Splat を scene object 扱いで試す。

- [ ] OBJ 読み込みを軽量なアクセサリ / 背景モデル用途として試せるか確認
- [ ] MTL / texture path 解決の負荷を確認
- [ ] OBJ は MMD アクセサリ相当の scene object として扱う
- [ ] OBJ は自前モーション対象にせず、必要なら PMX ダミーボーンや任意ボーンへ親付けして動かす
- [ ] OBJ の transform / 表示状態 / 読み込み元パスを project 保存できるか確認
- [ ] OBJ の材質は Toon 変換を急がず、まず Standard / PBR のまま表示できるか確認
- [ ] 点群 / Gaussian Splat 系の Babylon.js 9 系サポート状況を確認
- [ ] `.ply` / `.splat` / `.spz` / `.sog` の扱いを調査
- [ ] 点群 / Gaussian Splat は編集対象ではなく、背景・参照・ステージ素材、またはアクセサリ相当の scene object として扱う
- [ ] 点群 / Gaussian Splat も自前モーション対象にせず、必要なら PMX ダミーボーンや任意ボーンへ親付けして動かす
- [ ] 点群 / Gaussian Splat にメイン平行光源を当てるかどうかを選べる形で検証する
- [ ] 点群 / Gaussian Splat は撮影時の光が焼き込まれている可能性が高いため、ライト影響は既定 OFF 候補にする
- [ ] ライト影響 ON 時は、MMD のメイン平行光源と shadow casting の見た目が破綻しないか確認する
- [ ] 点群 / Gaussian Splat の transform / 表示状態 / 読み込み元パスを project 保存できるか確認
- [ ] GLTF / GLB で詰まった経緯を再確認し、同じ統合問題を踏まえて判断する

v0.2 の最小 PoC:

- OBJ 1 ファイルを scene object として読み込み、移動・回転・スケールできる
- OBJ を親モデル / 親ボーンへ追従させられる
- OBJ の project 保存 / 再読み込みができる
- `.ply` または `.splat` 1 ファイルを背景参照として読み込み、表示 / 非表示を切り替えられる
- 点群 / Gaussian Splat のライト影響 ON / OFF を切り替えられる
- PMX / VMD 再生、Frame Graph、接地影補助に干渉しない

v0.2 ではやらないこと:

- OBJ / 点群を MMD モーション対象にする
- OBJ / 点群を VMD 書き出し対象にする
- OBJ / 点群用の独自キーフレームトラックを作る
- OpenPBR / カスタムシェーダー規格対応まで広げる
- メッシュ編集、点群編集、スキャンデータ編集を行う

関連メモ:

- [glb-loading-investigation-2026-04-01.md](./glb-loading-investigation-2026-04-01.md)
- [generic-object-panel-design.md](./generic-object-panel-design.md)
- [mmd-project-positioning-note.md](./mmd-project-positioning-note.md)

## 初期スコープ

### 依存更新

- [x] Babylon.js 9 系への更新
- [x] `babylon-mmd` 1.2 系への更新
- [ ] Electron 更新の検証
- [ ] Vite 更新の検証
- [ ] Vitest 更新の検証
- [ ] Electron Forge 周辺の更新可否確認
- [ ] `@electron/fuses` 2 系へ上げる必要があるか確認
- [ ] Vite / Vitest 更新検証を別ブランチまたは別コミットで切る
  - まず `vite@6.x + vitest@4.x` を試す
  - 通ったら `vite@8.x` へ進むか判断する
  - `lint / test:unit / package / smoke:launch` を通す
  - babylon-mmd MPR / SPR wasm URL 解決、`optimizeDeps.exclude`、WebGPU 起動を確認する
- [ ] `zustand` を dependencies に追加するか判断
- [ ] `tailwindcss` / `@tailwindcss/vite` を devDependencies に追加するか判断

### MMD 基本動作確認

- [x] 既存プロジェクトファイル読み込み
- [x] モデル表示
- [x] ダンスモーション再生
- [ ] PMX / PMD の新規読み込み
- [ ] VMD の新規読み込み
- [ ] VPD の新規読み込み
- [ ] カメラ VMD 読み込み
- [ ] 複数モデル同時読み込み
- [ ] `.x` アクセサリ読み込み

### MMD 相当タイムライン項目

- [ ] 現行の `model | camera` 中心の `timelineTarget` をどう拡張するか整理
- [ ] `property` トラックの表示 / IK プレビュー
- [ ] 照明トラック
- [ ] アクセサリ / scene object track
- [ ] セルフ影 / 影設定トラック
- [ ] 重力トラック
- [ ] `runtime-native tracks` と `modoki-owned tracks` の責務分離
- [ ] modoki-owned tracks の共通キーフレーム形式
- [ ] modoki-owned tracks の再生時評価経路
- [ ] modoki-owned tracks の保存 / 読み込み
- [ ] SQLite-backed undo/redo と modoki-owned tracks の接続可否

優先順位:

1. 照明トラック
2. アクセサリ / scene object track
3. Property / IK
4. セルフ影 / 影設定
5. 重力

関連メモ:

- [mmd-timeline-track-scope-note-2026-04-20.md](./mmd-timeline-track-scope-note-2026-04-20.md)
- [timeline-spec.md](./timeline-spec.md)
- [keyframe-storage-spec.md](./keyframe-storage-spec.md)
- [generic-object-panel-design.md](./generic-object-panel-design.md)

### babylon-mmd と MMD_modoki の責務分担

babylon-mmd が提供するタイムライン / animation runtime は、基本的に PMX/PMD モデルと MMD カメラのモーションを扱うためのものとして見る。

babylon-mmd 側に乗せる対象:

- PMX / PMD ボーン
- PMX / PMD モーフ
- Property / IK のうち、babylon-mmd の `MmdPropertyAnimationTrack` と整合する範囲
- MMD カメラ

MMD_modoki 側で自前実装する対象:

- 照明
- セルフ影 / 影設定
- 重力
- アクセサリ
- scene object
- 将来の OBJ / 点群など、MMD モデルではないオブジェクト

このため、v0.2 で増やす非 PMX / 非カメラ項目は `modoki-owned tracks` として扱う。

`modoki-owned tracks` で必要になるもの:

- キーフレーム格納形式
- 補間またはステップ評価の仕様
- 現在フレームの値評価
- scene / UI への値反映
- プロジェクト保存 / 読み込み
- undo/redo との接続
- タイムライン表示上のカテゴリ・色・選択挙動

実装時は、babylon-mmd の runtime に無理に押し込まず、`MMD_modoki` 側で frame evaluation service のような層を持つ案を第一候補にする。

### 編集機能確認

- [ ] タイムライン表示
- [ ] フレームシーク
- [ ] ボーン選択同期
- [ ] ボーンギズモ操作
- [ ] ボーンキー登録
- [ ] カメラキー登録
- [ ] 補間カーブ表示
- [ ] 補間カーブ編集
- [ ] プロジェクト保存
- [ ] プロジェクト再読み込み

### 描画・エフェクト確認

- [ ] WebGPU 起動
- [ ] WebGL2 フォールバック
- [ ] Frame Graph 経由の基本描画
- [ ] Frame Graph 経由の DoF
- [ ] Frame Graph 経由の Bloom / LUT 順序比較
- [ ] Dynamic IBL Shadows の接地影確認
- [ ] Dynamic IBL Shadows を床・ステージ受け側限定で確認
- [ ] MirroringFloor / MirroringPlane の表示・出力確認
- [ ] SSR / SSR 用材質プリセットの表示・出力確認
- [ ] MMD 材質表示
- [ ] toon / sphere / texture 表示
- [ ] 透過材質表示
- [ ] 影表示
- [ ] SSAO
- [ ] DoF
- [ ] LUT
- [ ] Fog
- [ ] LuminousGlow
- [ ] 背景画像
- [ ] 背景動画

### 物理確認

- [x] 物理 ON / OFF
- [x] Bullet SPR の現状確認
- [x] Bullet MPR / Worker 物理の導入
- [x] Bullet MPR 失敗時の SPR fallback
- [x] Buffered 評価の導入可否
- [x] 再生開始時の安定性
- [x] シーク後の安定性
- [x] Home / End 後の安定性
- [x] ギズモ操作後の安定性
- [x] 剛体モード 0 / 1 / 2 の比較
- [x] 重めモデル 1 体での FPS / physics time 比較
- [x] 複数モデルでの FPS / physics time 比較
- [x] 物理 ON のまま長時間再生したときの安定性

残す課題:

- FPS 主因ではないため、以後は物理単体の最適化より、描画側ボトルネックと組み合わせた比較に回す
- 最重量モデルは既知の極端ケースとして扱い、主要な v0.2 完了条件にはしない

### 出力確認

- [ ] PNG 連番出力
- [ ] WebM 出力
- [ ] UI 非表示モードでの出力
- [ ] カメラ VMD 適用時の出力
- [ ] 長めのモーション出力

## 既に確認したこと

2026-04-27 時点:

- `@babylonjs/core` / `gui` / `loaders` を `9.2.0` に更新
- `babylon-mmd` を `^1.2.0` に更新
- `babylon-mmd@1.2.0` の peer dependency は `@babylonjs/core@^9.2.0`
- 初期検証は最新の Babylon 9.4 系ではなく、検証済みベースに寄せて 9.2.0 を使う
- `babylon-mmd@1.2.0` は Babylon.js 9.2.0 の skinning shader 変更に合わせた outline renderer 対応を含む
- `babylon-mmd@1.2.0` は Rust 2024 / LLVM 22 への更新と、Disposable pattern の見直しを含む

確認済みコマンド:

```powershell
npm.cmd run lint
npm.cmd run test:unit
npm.cmd run package
npm.cmd run smoke:launch
```

結果:

- lint は既存 warning のみ
- unit test は 31 件成功
- package は Windows x64 向けに成功
- smoke は `engine=WebGPU` まで到達

手動確認:

- 既存プロジェクトファイルから読み込み
- モデル表示
- ダンスモーション再生

結果:

- 目立つ問題なし

詳細:

- [dependency-upgrade-v0.2-note-2026-04-27.md](./dependency-upgrade-v0.2-note-2026-04-27.md)

## リスク

- Babylon 9 系では、材質・ポストエフェクト・WebGPU 周辺の細かい挙動差が後から見つかる可能性がある
- `babylon-mmd` 更新により、物理・モーフ・MMD ランタイム内部の挙動差が出る可能性がある
- Electron / Vite / Forge を同時に上げると、ビルド不具合と描画不具合の切り分けが難しくなる
- `@electron/fuses` 2 系は、現行の `@electron-forge/plugin-fuses@7.11.1` の peer dependency と合わない可能性がある
- Babylon 公式機能へ置き換える場合、起動確認だけでは足りない。MMD Standard の Toon 寄せ合成や既存 project の保存値互換を失う可能性がある
- SQLite WASM を undo/redo の中核にすると、操作粒度や副作用分離の問題を DB で隠してしまう可能性がある。そのため、v0.2.0 では Action / Command / in-memory HistoryManager を先に評価する
- 高頻度入力ログを雑に SQLite へ流すと、描画や再生の負荷調査そのものに影響する可能性がある
- OBJ や点群読み込みは、MMD 本体機能ではなく scene object / accessory 寄りの扱いになる。タイムラインや材質 UI に混ぜると scope が膨らむ
- GLTF / GLB で詰まったように、Babylon 側で読み込めることと MMD_modoki の scene / WebGPU / WGSL / UI に統合できることは別問題
- 照明・アクセサリ・セルフ影・重力は babylon-mmd の外側で評価する `modoki-owned tracks` になりやすい。タイムライン UI に出すだけでなく、保存・再生時評価・undo/redo の責務を決める必要がある
- Action 管理を入れずに shortcut custom / gamepad / MIDI を増やすと、入力経路ごとの挙動差とテスト漏れが増える可能性がある
- Zustand に runtime object や大きな animation buffer を入れると、保存 / 読み込みや Babylon runtime との同期問題が増える
- Tailwind CSS を全面移行として始めると、見た目変更と実行経路変更が混ざり、回帰原因を切り分けにくい

## 次の判断

Babylon 9.2 + babylon-mmd 1.2 の実モデル確認と Worker 対応物理は一段落した。次は Frame Graph 標準描画パイプライン PoC を優先する。

Frame Graph は v0.2 の描画標準候補として扱い、まず DoF / Bloom / LUT / SSAO の順序を動かせる最小構成を作る。

ただし、MMD 材質・outline・透過・WebGL2 fallback が崩れる場合は、旧描画経路を退避経路として残す。

DoF は既存計測で重いケースが見えているが、Frame Graph 化で経路そのものを触る可能性が高いため、現行 LensRenderingPipeline だけの細かい最適化は後回しにする。

影は描画負荷への影響が大きい。既定の影描画範囲縮小とモデル単位の遮蔽影 OFF を入れたので、以後は必要に応じてプロジェクト単位の運用で逃がす。

既存の設計メモ:

- [physics-worker-implementation-plan.md](./physics-worker-implementation-plan.md)

Action / Command 管理は、v0.2.0 の編集基盤整理として優先度を上げる。まずは keyframe add / delete / 1f nudge を対象にし、button / shortcut / timeline 操作を同じ Action 経路へ寄せる。Action 単位の Vitest を増やし、in-memory HistoryManager + command diff で undo/redo の最小 PoC を作る。

Zustand は、UI / editor state の lightweight snapshot 用として検討する。selection、editing mode、dirty、`canUndo` / `canRedo` などに限定し、Babylon object、MMD model 実体、大きな animation buffer、project save format は入れない。

SQLite WASM は、v0.2.0 では undo/redo の中核にしない。DB を入れても undo/redo の本質である「1 操作の粒度」「逆操作」「副作用の整合」は消えないため、まずは既存の [undo-redo-investigation.md](./undo-redo-investigation.md) にある `HistoryManager + command + 最小差分` 方式を優先する。SQLite WASM は必要になった場合に、`input_event` / `command_event` / `error_event` の観測ログやクラッシュ復元の後続 PoC として扱う。

Tailwind CSS は、UI のガワ整理を進める段階で Action 化済み UI または設定画面から段階導入する。全面移行から始めず、相対値 / 絶対値の混在や似た component の散らばりを小さい範囲から整理する。

Electron / Vite major update は、Babylon / babylon-mmd 更新が一段落しているなら検証タイミングとして悪くない。ただし Frame Graph や Action / Zustand / Tailwind の実装と混ぜると切り分けが難しいため、別コミットで `vite@6.x + vitest@4.x` を先に確認し、通ったら `vite@8.x` へ進むか判断する。

## 2026-07-01 FrameGraph / PostFX 現行補足

このメモ内の FrameGraph 記述は、2026-04 から 2026-06 にかけての計画・判断が混ざっている。2026-07-01 時点では、FrameGraph は v0.2 の描画標準候補として、post effect stack を中心に実装が進んでいる。

現在の状態:

- MMD scene render 全体の置き換えではなく、既存 scene color を取り込む post effect backend。
- `frameGraphPostStack` で順序と enabled を保存する。
- 個別 ON/OFF はパラメーター値とは分離し、OFF にしても効果設定を保持する。
- `Offset Rim -> Bloom` のような順序効果は実機で確認済み。
- `Offset Shadow` / `Offset Rim` は実験的だが、MMD 向けスタイライズ効果として stack に入っている。
- Luminous は AutoLuminous 完全互換ではなく、材質 / luminous mask ベースの FrameGraph stack 効果として扱う。
- WebGPU FrameGraph の texture 依存は build 後に固定されるため、順序 / enabled 変更は live reconnect ではなく backend rebuild で扱う。

未解決として、rapid reorder / toggle の debounce、Offset Shadow / Rim の model mask、Offset Rim の thickness 品質、外部 LUT と PNG / WebM 出力反映の確認が残る。

現行仕様の入口は [FrameGraph Post Stack 現行仕様メモ 2026-07-01](./framegraph-post-stack-current-spec-2026-07-01.md) とする。
